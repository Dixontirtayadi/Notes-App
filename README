This is a project that uses Node.js To run the program, 
type in "node app.js" in terminal with some kind of command as argument.
Remember to be in the Notes-App directory before running "node app.js"
For help type "node app.js --help" in the terminal.
